supergraph:
  listen: 0.0.0.0:4000

health_check:
  listen: 0.0.0.0:4000
  enabled: true

sandbox:
  enabled: true

homepage:
  enabled: false

headers:
  all:
    request:
      - propagate:
          named: "x-user-id"
      - propagate:
          named: "x-user-tier"
      - propagate:
          named: "x-profile-id"

limits:
  max_depth: 10
  max_aliases: 15

cors:
  origins:
    - "*"
  allow_headers:
    - content-type
    - authorization
  methods:
    - GET
    - POST
    - OPTIONS
